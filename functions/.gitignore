# Compiled JavaScript files
lib/

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/

# Per-deployment config is NEVER committed -- the repo is deployment agnostic.
# Each deploy target needs a local .env.<projectId>; see .env.example.
.env
.env.*
!.env.example
